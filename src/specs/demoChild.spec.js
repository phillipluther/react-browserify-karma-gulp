'use strict';

var demoChild = require('../scripts/demoChild.jsx');

describe('demoChild.js', function() {
    it('should be able to do basic math', function() {
        expect(1 + 1).toEqual(2);
    });
});