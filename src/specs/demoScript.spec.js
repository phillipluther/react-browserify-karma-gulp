'use strict';

var demoScript = require('../scripts/demoScript.jsx');

describe('demoScript.js', function() {
    it('should know absolute truth', function() {
        expect(true).toBe(true);
    });
});